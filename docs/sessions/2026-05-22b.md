# Session 2026-05-22b

## Changes Made

### Web app — schedule data replacement
- `js/data.js` — 14 new CATEGORIES (work/routine/hygiene/fitness/jobs/project/cyber/
  game/school/church/chore/winddown/sleep/free) with correct hex colours from v5 HTML;
  added WEEKLY_SCHEDULE keyed Mon–Sun from weekly_schedule_v5_chores_teas.html;
  cleared SPECIFIC_SCHEDULES (Security+ exam data removed); bumped to v1.3.0
- `js/schedule.js` — `getScheduleForDate` now falls back to WEEKLY_SCHEDULE when no
  date-specific override exists; `calculateStats` updated to track jobs/project/cyber
- `js/ui.js` — fallback category `personal` → `free` (4 occurrences);
  stats DOM keys updated to match new categories
- `index.html` — stats cards updated: Study→Project (💻), Portfolio→Cyber (🔐); Jobs unchanged
- Committed `322dd70`, pushed to `main`, GitHub Pages deployed ✅

### Mobile app — iOS build investigation (not committed)
- Fixed WebView URL: `Belific/` → `belific/` (lowercase)
- Fixed package versions for SDK 54: expo-notifications `~0.32.0`, expo-linking `~8.0.12`,
  react-native-webview `13.15.0`, safe-area-context `~5.6.0`, datetimepicker `8.4.4`
- Fixed `NotificationBehavior` API: `shouldShowAlert` → `shouldShowBanner` + `shouldShowList`
- Added Podfile `post_install` fix for fmt/Clang C++20 consteval issue on Xcode 26
- Native build succeeded (0 errors), app installed on iPhone 17 Pro simulator
- Remaining JS runtime error: `ExpoLinking` native module missing — requires rebuild
  after `expo-linking` was added post-initial build (pod install done, rebuild pending)

## Pending Next Session
1. Rebuild mobile app (`npx expo run:ios`) after ExpoLinking pod install
2. Confirm WebView loads https://najeca.github.io/belific/ in simulator
3. Test notification permission prompt on launch
4. Commit all mobile changes (package.json, index.tsx, _layout.tsx, tsconfig.json)
5. Update docs/current-state-audit.md to reflect web schedule complete + mobile in progress

## Decisions
- WEEKLY_SCHEDULE is the new source of truth; SPECIFIC_SCHEDULES kept empty for one-off overrides
- Stats track Jobs / Project / Cyber (replaced Study / JobHunting / Portfolio)
- Streak at Risk notification fires unconditionally (web activity unreadable without modifying web JS)
