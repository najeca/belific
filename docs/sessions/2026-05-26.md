# Session 2026-05-26

## Changes Made

### iOS build — full pipeline resolved
- Fixed `react-dom` conflict: uninstalled, pinned `react@19.1.0`
- Installed `expo-modules-autolinking` explicitly (not hoisted in this single-package setup)
- Resolved `expo-font` autolinking silent skip: manual `pod 'ExpoFont'` in Podfile + `"expo-font"` in `app.json` plugins
- Resolved fmt / Clang 16 `consteval` build error: `base.h` gsub patch with `chmod` (load-bearing fix); `-DFMT_CONSTEVAL=constexpr` flag alone is overridden by the header
- Bumped iOS deployment target to 16.4 in three places: Podfile fallback, `app.json`, `xcodeproj` (4 occurrences via `sed`)
- Added Pods deployment target bump loop in `post_install`
- Release build compiled and installed on device `00008150-000438D40A92401C` (Jethro's iPhone)
- Note: device was locked at launch step — app installed successfully, launch blocked by lock screen only

### Docs — Landis Obsidian vault structure replicated
- Created `docs/architecture.md`
- Created `docs/UBIQUITOUS_LANGUAGE.md`
- Created `docs/cursor-audit.md` (empty, ready for Cursor)
- Created `docs/publishing-reference.md`
- Created `docs/decisions/001-webview-not-rewrite.md`
- Created `docs/decisions/002-local-notifications-only.md`
- Created `docs/decisions/003-no-supabase.md`
- Created `docs/decisions/004-ios-deployment-target-16-4.md`
- Created `docs/decisions/005-expo-font-manual-pod.md`
- Moved `IOS_BUILD_NOTES.md` from repo root → `docs/IOS_BUILD_NOTES.md`
- Updated `docs/CLAUDE.md`: corrected web URL to lowercase, added iOS stack facts, updated session protocol to reference all docs, added skills table
- Updated `docs/current-state-audit.md`: full status update, vault structure documented

### .claude/ — skills structure created (mirrors Landis)
- `.claude/settings.local.json`
- `.claude/skills/workflow/ios-build/SKILL.md` + `known-errors.md` + `pre-build-check.sh` + `patch-podfile.sh`
- `.claude/skills/workflow/session-close/SKILL.md` + `session-summary.sh`
- `.claude/skills/workflow/security-review/SKILL.md` + `security-scan.sh`
- `.claude/skills/workflow/testing/SKILL.md`
- `.claude/skills/features/notifications/SKILL.md`
- `.claude/skills/features/webview/SKILL.md`

### .gitignore
- Created `.gitignore`: node_modules, ios/, android/, .env, .DS_Store, docs/.obsidian/

## Pending Next Session
1. Unlock device and verify app launches correctly
2. Verify WebView loads `https://najeca.github.io/belific/` on device
3. Test notification permission prompt on first launch
4. Verify all three notification types schedule correctly
5. Commit all mobile source changes (package.json, Podfile, app.json, mobile code)
6. Set up EAS build profiles in `mobile/eas.json`

## Decisions
- iOS deployment target is 16.4 (required by expo-font 56.x) — documented in `decisions/004`
- `expo-font` must be manually podded forever — autolinking silently skips it — documented in `decisions/005`
- `--device <UDID>` not `--udid` — `--udid` flag does not exist in this Expo CLI version
- Streak at Risk fires unconditionally at 8 PM — web activity is unreadable from native (reaffirmed)
- `fmt` base.h gsub patch is the load-bearing Clang 16 fix — the compiler flag alone is overridden by the header redefining `FMT_CONSTEVAL`
