# Session 2026-05-22

## Changes Made
- Overwrote `docs/CLAUDE.md` — replaced copied Landis placeholder with Belific-specific
  architecture decisions, notification types, quality gates, and domain language
- Created `docs/current-state-audit.md` — captures project starting point before iOS work
- Committed both files: `c775776`

## Pending Next Session
1. Initialise Expo app inside repo (e.g. `mobile/`)
2. Configure WebView to load https://najeca.github.io/Belific/
3. Install and configure expo-notifications
4. Wire up three notification types:
   - Custom reminders (user-defined time + message)
   - Weekly summary (Sunday 6 PM)
   - Streak at risk (daily 8 PM, conditional on no activity)
5. EAS build and test on physical iOS device

## Decisions
- Web app is the single source of UI truth — never rewrite, only wrap in WebView
- Notifications are local only — no backend required
- Polyfill order follows Landis convention (react-native-get-random-values first)
- Bottom tab bar only, babel.config.js required, useSafeAreaInsets() always
